# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Environment / secrets — never commit these
.env
.env.*
!.env.template
!.env.example

# Generated client sites (the Hunter and SaaS CLI write here).
# NOTE: corrupt-dealership-engine/dist/index.html is a tracked demo preview, so
# don't blanket-ignore dist/ — only the generated client output locations.
/prospects/
/projects/clients/

# OS / editor
.DS_Store
*.swp

# Private working docs — must never ship in a distributable/clonable repo.
# These carry live infrastructure detail (SSH/IPs), unpatched-vulnerability lists,
# client names, and internal business strategy. They stay local only.
*.local.md
ROADMAP.md
